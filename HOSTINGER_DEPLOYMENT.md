# دليل رفع ونشر متجر ميني بازار — Mini Bazaar على GitHub و Hostinger

تم تصميم وبرمجة المتجر الإلكتروني الفاخر **ميني بازار (Mini Bazaar)** ليكون متوافقاً 100% مع مستودعات **GitHub** والاستضافة على **Hostinger**.

---

## 1. محتويات المشروع وهيكليته

- **Framework**: React 19 + TypeScript + Vite.
- **Styling**: Tailwind CSS مع لوحة ألوان الهوية الفاخرة المعتمدة (Off-white, Linen, Camel/Gold, Deep Mocha).
- **Icons**: Lucide React.
- **Animations**: Motion (Framer Motion).
- **Build Output**: مجلد `dist/` يحتوي على ملفات ثابتة فائقة السرعة وخفيفة الحجم (`index.html`، ملفات JS و CSS المجمعة والمضغوطة) جاهزة للرفع مباشرة على أي استضافة ويب.

---

## 2. خطوات الرفع على مستودع GitHub (`mini-bazar`)

إذا قمت بإنشاء مستودع باسم `mini-bazar` على حسابك في GitHub، يمكنك ربط المشروع ورفعه عبر الأوامر التالية من سطر الأوامر في مجلد المشروع:

```bash
# 1. تهيئة المستودع المحلي (إذا لم تكن مهيأة مسبقاً)
git init

# 2. إضافة كافة ملفات المشروع
git add .

# 3. حفظ الالتزام الأول
git commit -m "feat: Initial commit for Mini Bazaar luxury store"

# 4. تعيين الفرع الرئيسي
git branch -M main

# 5. ربط المستودع بمستودع GitHub الخاص بك (استبدل YOUR_USERNAME باسم حسابك)
git remote add origin https://github.com/YOUR_USERNAME/mini-bazar.git

# 6. رفع الكود
git push -u origin main
```

---

## 3. خطوات النشر على استضافة Hostinger

لديك خياران سهلان لنشر الموقع على استضافة هوستينجر:

### الخيار الأول: النشر عبر Git المباشر في Hostinger (الأسهل والأسرع)
1. سجّل الدخول إلى لوحة تحكم **Hostinger hPanel**.
2. توجّه إلى موقعك واختر **Advanced** > **GIT**.
3. في حقل **Repository**: ضع رابط مستودعك: `https://github.com/YOUR_USERNAME/mini-bazar.git`.
4. في حقل **Branch**: ضع `main`.
5. اختر **Install** أو **Deploy**.
6. داخل Hostinger يمكنك تشغيل أمر البناء `npm run build` أو رفع محتويات مجلد `dist/` مباشرة.

---

### الخيار الثاني: بناء المشروع محلياً ورفع مجلد `dist` إلى `public_html`
1. في سطر الأوامر، قم بتشغيل أمر البناء:
   ```bash
   npm run build
   ```
2. سيتم إنشاء مجلد جديد باسم `dist/`.
3. افتح **File Manager** (مدير الملفات) في Hostinger.
4. ادخل إلى المجلد الرئيسي لموقعك: `public_html`.
5. ارفع جميع الملفات والمجلدات الموجودة **داخل** مجلد `dist/` مباشرة إلى `public_html`.
6. سيصبح متجر ميني بازار متاحاً وفورياً على نطاقك الخاص!

---

## 4. ملف `.htaccess` الموصى به لـ Hostinger (لإعادة التوجيه السلس للروابط)
إذا أردت دعم التوجيه وإعادة التحميل التلقائي في Hostinger، يمكنك إنشاء ملف باسم `.htaccess` داخل مجلد `public_html`:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

---

## 5. ميزات المتجر المكتملة

- **شريط إعلاني متحرك فاخر** مع إمكانية التعديل والإخفاء من لوحة التحكم.
- **هيدر فاخر متكامل** مع محرك بحث فوري، تصنيفات منتقاة، سلة منبثقة، وقائمة سريعة.
- **هيرو دائري متصل (Seamless Carousel)** مع تحكم بالسرعة ودوران مستمر دون فراغات.
- **أقسام رئيسية**: حقائب يد فاخرة، ساعات ومجوهرات، إكسسوارات ونظارات، عطور ومسك، هدايا وبوكسات.
- **بطاقات منتجات راقية**: استعراض الأسعار، الخصومات، التقييم، حالة التوفر، وزر السلة.
- **نافذة تفاصيل المنتج المتقدمة**: معرض صور، خيارات المتغيرات، كميات، وتواصل واتساب بنقرة واحدة.
- **سلة تسوق ودرج جانبي تفاعلي** مع مؤشر الشحن المجاني.
- **صفحة إتمام الطلب المعتمدة (Checkout)**: نموذج شحن متكامل، اختيار وسيلة الشحن، والدفع المعتمد (تحويل بنكي بالآيبان أو دفع عند الاستلام).
- **شاشة نجاح الطلب والفاتورة المعتمدة**: قابلة للطباعة وتدعم الإرسال الفوري عبر واتساب.
- **لوحة تحكم وتخصيص كاملة للمدير (Admin Dashboard)**:
  - إدارة الطلبات وتغيير الحالات مع سجل تدقيق وتاريخ (Audit Logs).
  - إدخال طلبات واتساب اليدوية.
  - إضافة وتعديل وحذف المنتجات والأسعار والتصنيفات.
  - محرر شرائح الهيرو (العناوين، الصور، الأزرار).
  - نشر المسودة أو استعادة الإعدادات الافتراضية بنقرة واحدة.
